/* 我的工资 - adminweixin 风格 */
.page-container {
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
  padding-bottom: 40rpx;
}

.top-nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  padding: 20rpx 0;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.1);
}

.nav-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 32rpx;
  gap: 24rpx;
  min-height: 88rpx;
}

.nav-title {
  flex: 1;
  min-width: 0;
  font-size: 36rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #667eea, #764ba2);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}

.nav-actions {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex-shrink: 0;
}

.nav-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #667eea, #764ba2);
  box-shadow: 0 4rpx 12rpx rgba(102, 126, 234, 0.3);
  border: none;
}
.nav-btn:active { transform: scale(0.96); }

.nav-btn-text {
  height: 56rpx;
  padding: 0 24rpx;
  border-radius: 28rpx;
}
.nav-btn-label {
  font-size: 26rpx;
  color: #fff;
  font-weight: 600;
}

.nav-btn-icon {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
}
.nav-icon {
  font-size: 36rpx;
  color: #fff;
  line-height: 1;
}

.main-content { padding-top: 120rpx; padding-left: 40rpx; padding-right: 40rpx; position: relative; z-index: 1; }

.filter-bar {
  background: rgba(255,255,255,0.95);
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,0.08);
}
.filter-row { display: flex; align-items: center; margin-bottom: 20rpx; }
.filter-row:last-of-type { margin-bottom: 0; }
.filter-label { width: 140rpx; flex-shrink: 0; font-size: 26rpx; color: #666; }
.filter-value {
  flex: 1;
  padding: 16rpx 20rpx;
  background: #f5f5f5;
  border-radius: 12rpx;
  font-size: 26rpx;
  color: #333;
}
.filter-value.small { flex: none; min-width: 160rpx; }
.filter-range { display: flex; align-items: center; gap: 16rpx; flex-wrap: wrap; }
.range-sep { font-size: 24rpx; color: #999; }
.filter-input {
  flex: 1;
  padding: 16rpx 20rpx;
  background: #f5f5f5;
  border-radius: 12rpx;
  font-size: 26rpx;
}
.filter-actions { display: flex; gap: 24rpx; margin-top: 24rpx; }
.btn-filter, .btn-clear {
  flex: 1;
  height: 72rpx;
  line-height: 72rpx;
  border-radius: 36rpx;
  font-size: 28rpx;
  border: none;
}
.btn-filter { background: linear-gradient(135deg, #667eea, #764ba2); color: #fff; }
.btn-clear { background: #f0f0f0; color: #666; }

.wage-overview {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 32rpx;
  padding: 40rpx;
  margin-bottom: 30rpx;
  box-shadow: 0 20rpx 60rpx rgba(0, 0, 0, 0.15);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.overview-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24rpx; }
.overview-title { font-size: 32rpx; font-weight: 700; color: #333; }
.overview-period { font-size: 26rpx; color: #666; }

.overview-stats { display: grid; grid-template-columns: 1fr 1fr; gap: 24rpx; }
.overview-item {
  text-align: center;
  padding: 24rpx;
  background: rgba(102, 126, 234, 0.08);
  border-radius: 20rpx;
  border: 1rpx solid rgba(102, 126, 234, 0.15);
}
.overview-number { display: block; font-size: 36rpx; font-weight: 800; color: #667eea; margin-bottom: 8rpx; }
.overview-label { font-size: 24rpx; color: #666; }

.loading-wrap { text-align: center; padding: 60rpx; color: #fff; font-size: 28rpx; }
.empty-wrap { text-align: center; padding: 80rpx 40rpx; }
.empty-icon { font-size: 80rpx; margin-bottom: 24rpx; }
.empty-title { display: block; font-size: 32rpx; color: #fff; font-weight: 600; margin-bottom: 12rpx; }
.empty-desc { display: block; font-size: 26rpx; color: rgba(255,255,255,0.8); margin-bottom: 32rpx; }
.empty-action {
  display: inline-block;
  padding: 20rpx 48rpx;
  background: rgba(255,255,255,0.9);
  border-radius: 40rpx;
  color: #667eea;
  font-size: 28rpx;
  font-weight: 600;
}
.action-text { color: inherit; }

.wage-list { display: flex; flex-direction: column; gap: 24rpx; }
.wage-card {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 24rpx;
  padding: 28rpx;
  box-shadow: 0 12rpx 40rpx rgba(0, 0, 0, 0.12);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.wage-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; }
.wage-title { flex: 1; margin-right: 16rpx; }
.product-name { font-size: 30rpx; font-weight: 700; color: #333; }
.model-name { font-size: 26rpx; color: #666; }
.wage-amount { flex-shrink: 0; }
.amount-text { font-size: 32rpx; font-weight: 800; color: #667eea; }

.wage-info { display: flex; flex-wrap: wrap; gap: 16rpx 24rpx; margin-bottom: 12rpx; }
.info-row { display: flex; align-items: center; gap: 8rpx; }
.info-label { font-size: 24rpx; color: #999; }
.info-value { font-size: 24rpx; color: #333; }

.wage-footer { padding-top: 12rpx; border-top: 1rpx solid #f0f0f0; }
.time-text { font-size: 24rpx; color: #999; }

.load-more { text-align: center; padding: 32rpx; color: #fff; font-size: 28rpx; }
