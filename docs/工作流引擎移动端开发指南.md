# 工作流引擎移动端开发指南

## 概述

工作流引擎提供了完整的API接口，支持移动端（小程序、App、H5）审批功能。

## 移动端API接口

### 1. 审批中心接口

#### 1.1 获取待审批列表
```
GET /api/workflow/pending
Headers:
  Authorization: Bearer {token}

Response:
{
    "code": 0,
    "msg": "success",
    "data": {
        "list": [
            {
                "record_id": 1,
                "instance_id": 1,
                "instance_no": "WF20260414123456789",
                "business_title": "订单号：ORD001",
                "node_name": "经理审批",
                "initiator": "张三",
                "create_time": "2026-04-14 10:00:00"
            }
        ],
        "total": 10
    }
}
```

#### 1.2 获取审批详情
```
GET /api/workflow/detail/{instance_id}
Headers:
  Authorization: Bearer {token}

Response:
{
    "code": 0,
    "msg": "success",
    "data": {
        "instance": {...},
        "records": [...],
        "current_node": "manager_approval",
        "can_approve": true
    }
}
```

#### 1.3 提交审批
```
POST /api/workflow/approve
Headers:
  Authorization: Bearer {token}
Body:
{
    "record_id": 1,
    "action": "approve",  // approve/reject/transfer
    "comment": "同意",
    "transfer_to": null  // 转交时必填
}

Response:
{
    "code": 0,
    "msg": "操作成功"
}
```

#### 1.4 撤回审批
```
POST /api/workflow/withdraw
Headers:
  Authorization: Bearer {token}
Body:
{
    "instance_id": 1
}

Response:
{
    "code": 0,
    "msg": "撤回成功"
}
```

### 2. 统计接口

#### 2.1 获取待审批数量
```
GET /api/workflow/pending_count
Headers:
  Authorization: Bearer {token}

Response:
{
    "code": 0,
    "msg": "success",
    "data": {
        "pending": 5,
        "approved": 20,
        "my": 15
    }
}
```

## 微信小程序开发

### 1. 审批列表页面

```javascript
// pages/approval/list.js
Page({
  data: {
    activeTab: 'pending',
    pendingList: [],
    approvedList: [],
    myList: []
  },

  onLoad() {
    this.loadPendingList();
  },

  onTabChange(e) {
    const tab = e.detail.name;
    this.setData({ activeTab: tab });

    if (tab === 'pending') {
      this.loadPendingList();
    } else if (tab === 'approved') {
      this.loadApprovedList();
    } else if (tab === 'my') {
      this.loadMyList();
    }
  },

  loadPendingList() {
    wx.request({
      url: 'https://your-domain.com/api/workflow/pending',
      header: {
        'Authorization': 'Bearer ' + wx.getStorageSync('token')
      },
      success: (res) => {
        if (res.data.code === 0) {
          this.setData({ pendingList: res.data.data.list });
        }
      }
    });
  },

  goDetail(e) {
    const instanceId = e.currentTarget.dataset.id;
    wx.navigateTo({
      url: '/pages/approval/detail?id=' + instanceId
    });
  }
});
```

### 2. 审批详情页面

```javascript
// pages/approval/detail.js
Page({
  data: {
    instanceId: 0,
    detail: null,
    comment: '',
    canApprove: false
  },

  onLoad(options) {
    this.setData({ instanceId: options.id });
    this.loadDetail();
  },

  loadDetail() {
    wx.request({
      url: 'https://your-domain.com/api/workflow/detail/' + this.data.instanceId,
      header: {
        'Authorization': 'Bearer ' + wx.getStorageSync('token')
      },
      success: (res) => {
        if (res.data.code === 0) {
          this.setData({
            detail: res.data.data,
            canApprove: res.data.data.can_approve
          });
        }
      }
    });
  },

  onCommentInput(e) {
    this.setData({ comment: e.detail.value });
  },

  onApprove() {
    if (!this.data.comment) {
      wx.showToast({ title: '请填写审批意见', icon: 'none' });
      return;
    }

    wx.request({
      url: 'https://your-domain.com/api/workflow/approve',
      method: 'POST',
      header: {
        'Authorization': 'Bearer ' + wx.getStorageSync('token')
      },
      data: {
        record_id: this.data.detail.record_id,
        action: 'approve',
        comment: this.data.comment
      },
      success: (res) => {
        if (res.data.code === 0) {
          wx.showToast({ title: '审批成功', icon: 'success' });
          setTimeout(() => {
            wx.navigateBack();
          }, 1500);
        } else {
          wx.showToast({ title: res.data.msg, icon: 'none' });
        }
      }
    });
  },

  onReject() {
    if (!this.data.comment) {
      wx.showToast({ title: '请填写拒绝原因', icon: 'none' });
      return;
    }

    wx.showModal({
      title: '确认拒绝',
      content: '确定要拒绝此审批吗？',
      success: (res) => {
        if (res.confirm) {
          wx.request({
            url: 'https://your-domain.com/api/workflow/approve',
            method: 'POST',
            header: {
              'Authorization': 'Bearer ' + wx.getStorageSync('token')
            },
            data: {
              record_id: this.data.detail.record_id,
              action: 'reject',
              comment: this.data.comment
            },
            success: (res) => {
              if (res.data.code === 0) {
                wx.showToast({ title: '已拒绝', icon: 'success' });
                setTimeout(() => {
                  wx.navigateBack();
                }, 1500);
              }
            }
          });
        }
      }
    });
  }
});
```

### 3. 审批列表页面

```html
<!-- pages/approval/list.wxml -->
<van-tabs active="{{ activeTab }}" bind:change="onTabChange">
  <van-tab title="待审批" name="pending">
    <van-cell-group>
      <van-cell
        wx:for="{{pendingList}}"
        wx:key="id"
        title="{{item.business_title}}"
        label="{{item.node_name}} · {{item.create_time}}"
        is-link
        bind:click="goDetail"
        data-id="{{item.instance_id}}"
      />
    </van-cell-group>
  </van-tab>
  <van-tab title="已审批" name="approved">
    <van-cell-group>
      <van-cell
        wx:for="{{approvedList}}"
        wx:key="id"
        title="{{item.business_title}}"
        label="{{item.action}} · {{item.action_time}}"
      />
    </van-cell-group>
  </van-tab>
  <van-tab title="我发起的" name="my">
    <van-cell-group>
      <van-cell
        wx:for="{{myList}}"
        wx:key="id"
        title="{{item.business_title}}"
        label="{{item.status}} · {{item.create_time}}"
        is-link
        bind:click="goDetail"
        data-id="{{item.id}}"
      />
    </van-cell-group>
  </van-tab>
</van-tabs>
```

### 4. 审批详情页面

```html
<!-- pages/approval/detail.wxml -->
<van-panel title="审批详情" wx:if="{{detail}}">
  <!-- 业务信息 -->
  <van-cell-group>
    <van-cell title="实例编号" value="{{detail.instance.instance_no}}" />
    <van-cell title="业务标题" value="{{detail.instance.business_title}}" />
    <van-cell title="当前状态" value="{{detail.instance.status}}" />
  </van-cell-group>

  <!-- 审批记录 -->
  <van-steps direction="vertical" active="-1">
    <van-step wx:for="{{detail.records}}" wx:key="id">
      <view slot="title">
        {{item.node_name}}
        <van-tag type="{{item.status === 'approved' ? 'success' : item.status === 'rejected' ? 'danger' : 'warning'}}">
          {{item.status}}
        </van-tag>
      </view>
      <view slot="desc">
        <view>审批人：{{item.approver_name}}</view>
        <view wx:if="{{item.comment}}">意见：{{item.comment}}</view>
        <view>时间：{{item.action_time || '待审批'}}</view>
      </view>
    </van-step>
  </van-steps>

  <!-- 审批操作 -->
  <van-cell-group wx:if="{{canApprove}}">
    <van-field
      value="{{comment}}"
      label="审批意见"
      placeholder="请输入审批意见"
      bind:change="onCommentInput"
    />
    <van-cell title="操作">
      <view slot="right-icon">
        <van-button type="success" size="small" bind:click="onApprove">通过</van-button>
        <van-button type="danger" size="small" bind:click="onReject">拒绝</van-button>
      </view>
    </van-cell>
  </van-cell-group>
</van-panel>
```

## App开发（UniApp）

### 1. 审批页面（Vue组件）

```vue
<!-- pages/approval/list.vue -->
<template>
  <view class="approval-list">
    <uni-segmented-control
      :current="currentTab"
      :values="['待审批', '已审批', '我发起的']"
      @clickItem="onTabChange"
    />

    <view class="list">
      <view
        class="item"
        v-for="item in list"
        :key="item.id"
        @click="goDetail(item)"
      >
        <view class="title">{{ item.business_title }}</view>
        <view class="info">
          <text>{{ item.node_name }}</text>
          <text class="time">{{ item.create_time }}</text>
        </view>
      </view>
    </view>
  </view>
</template>

<script>
export default {
  data() {
    return {
      currentTab: 0,
      list: []
    };
  },

  onLoad() {
    this.loadList();
  },

  methods: {
    onTabChange(e) {
      this.currentTab = e.currentIndex;
      this.loadList();
    },

    loadList() {
      const url = this.currentTab === 0 ? '/api/workflow/pending' :
                  this.currentTab === 1 ? '/api/workflow/approved' :
                  '/api/workflow/my_instances';

      uni.request({
        url: this.$baseUrl + url,
        header: {
          'Authorization': 'Bearer ' + uni.getStorageSync('token')
        },
        success: (res) => {
          if (res.data.code === 0) {
            this.list = res.data.data.list;
          }
        }
      });
    },

    goDetail(item) {
      uni.navigateTo({
        url: '/pages/approval/detail?id=' + item.instance_id
      });
    }
  }
};
</script>
```

## H5移动端开发

### 1. 使用Vant UI组件库

```html
<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/vant@2.12/lib/index.css">
  <script src="https://cdn.jsdelivr.net/npm/vant@2.12/lib/vant.min.js"></script>
</head>
<body>
  <div id="app">
    <van-tabs v-model="activeTab" @change="onTabChange">
      <van-tab title="待审批" name="pending">
        <van-pull-refresh v-model="refreshing" @refresh="onRefresh">
          <van-list
            v-model="loading"
            :finished="finished"
            @load="onLoad"
          >
            <van-cell
              v-for="item in list"
              :key="item.id"
              :title="item.business_title"
              :label="item.node_name + ' · ' + item.create_time"
              is-link
              @click="goDetail(item)"
            />
          </van-list>
        </van-pull-refresh>
      </van-tab>
    </van-tabs>
  </div>

  <script>
    new Vue({
      el: '#app',
      data: {
        activeTab: 'pending',
        list: [],
        loading: false,
        finished: false,
        refreshing: false
      },
      methods: {
        onTabChange(name) {
          this.list = [];
          this.finished = false;
          this.onLoad();
        },
        onLoad() {
          // 加载列表数据
        }
      }
    });
  </script>
</body>
</html>
```

## 移动端特性

### 1. 消息推送

- **微信小程序**：使用模板消息
- **App**：使用第三方推送服务（极光、个推等）
- **H5**：使用Web Push API

### 2. 生物识别认证

```javascript
// 微信小程序生物识别
wx.startSoterAuthentication({
  requestAuthModes: ['fingerPrint'],
  challenge: 'challenge',
  authContent: '请验证身份进行审批',
  success: (res) => {
    // 验证成功，执行审批
    this.doApprove();
  }
});
```

### 3. 语音输入审批意见

```javascript
// 使用语音识别API
const recorderManager = wx.getRecorderManager();

recorderManager.onStop((res) => {
  wx.getRecorderManager().stop();
  // 识别语音转文字
  this.recognizeSpeech(res.tempFilePath);
});
```

### 4. 拍照附件

```javascript
// 拍照上传附件
wx.chooseImage({
  count: 1,
  sizeType: ['compressed'],
  sourceType: ['camera'],
  success: (res) => {
    this.uploadAttachment(res.tempFilePaths[0]);
  }
});
```

## 性能优化

1. **列表分页加载**：使用虚拟列表或分页加载
2. **图片懒加载**：审批记录中的头像等图片懒加载
3. **本地缓存**：缓存已审批的数据，减少请求
4. **离线支持**：使用Service Worker支持离线审批

## 安全建议

1. **Token管理**：定期刷新Token，安全存储
2. **数据加密**：敏感数据加密传输
3. **权限控制**：后端严格校验权限
4. **防重放攻击**：审批操作添加唯一标识
