/**
 * 支付网关配置
 */
(function () {
    var Controller = {
        index: function () {
            // 表格初始化逻辑已移至视图文件中
            console.log('Payment Config Index page loaded');
        }
    };
    window.__backendController = Controller;
})();
